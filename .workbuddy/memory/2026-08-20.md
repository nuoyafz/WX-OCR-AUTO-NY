# 2026-08-20 工作日志

## 任务1：分析用户本轮大改（6文件 +2568/-584）并跟进优化
用户本轮主要新增：群聊识别（ocr_engine V3 加权投票 + 成员昵称剥离）、发送者判定 V4（绿色气泡背景采样）、自己消息也入库、屏幕外保活/后台点击、Obsidian 同步改版。
分析结论（已交付用户）：
- P0（已修）：`main.py:1019` 调 `extractor.extract(..., extra={...})` 但 `extractor.extract` 签名无 `extra` 参数 → TypeError 被主循环 except 吞掉 → 当前窗口整条提取/重要/存储/Obsidian/自动回复链路全失效。修法：extractor 签名加 `extra=None` 接收。
- P1（尚未修）：增量（非最小化）路径 `main.py:852-856` 算出 `final_kind` 但**没回写 `current_chat_kind`**，导致前台模式群聊类型判定不生效（全量路径有回写）。
- P2（尚未修）：轮询 `main.py:1137` / 红点 `main.py:1396,1469` 分支仍用旧 `recognize+identify_senders`，未接入 V4/群聊增强。

## 任务2：修 P0 + 修"点A联系人显示全部消息"的 UI bug
- P0 修复：extractor.py `extract` 加 `extra=None` 参数。
- UI bug 根因：`msg_list_frame_inner` 把所有联系人消息混在同一 frame，点击会话卡只改标题不过滤。
- 修法（ui_app.py）：每条消息行记 `row._contact`；新增 `_apply_message_visibility(row)` / `_refresh_message_visibility()`；`_set_active_contact` 切换时调用刷新，只显示当前 `self._active_contact` 的消息（None 时全显）。物理顺序保持"最新置顶"，仅用 pack_forget/pack 切换可见性。
- 已 `git commit 79f4376`。

## 用户约定（新增）
- 用户要求：**每次更新代码前/后都要先 git 提交**（已遵行）。

## 待办（未做，用户未批准）
- P1 增量路径回写 current_chat_kind（一行代码）。
- P2 轮询/红点分支接入 V4 + 群聊增强（建议统一收口成 `_process_ocr_results` 入口，消除三套重复后处理）。
