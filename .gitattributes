# Windows 批处理必须是 GBK/CRLF 原样字节，禁止 git 任何换行/编码转换
# 原因：core.autocrlf 会把 CRLF 转 LF 存入仓库，GitHub zip 导出 LF-only
# 批处理导致 cmd 解析错乱乱码；GBK 中文又会被 git 判为 binary 使 eol 规则失效，
# 因此用 -text 强制原样存储（工作区是 CRLF，index 就存 CRLF）
*.bat -text
