# 产品需求文档（PRD）— 团队协作白板

| 项 | 内容 |
|---|---|
| 文档状态 | 草案 v1.0（待评审） |
| 产品 | B2B SaaS 主产品 · 内嵌协作能力 |
| 功能 | 团队协作白板（Team Whiteboard） |
| 负责人 | 产品经理（待填） |
| 创建日期 | 2026-08-24 |
| 目标发布 | Q2 交付 MVP，Q3 交付完整版 |
| 资源 | 1 前端工程师 + 1 后端工程师，全职约 3 个月 |

---

## 1. 问题陈述（Problem Statement）

我们的产品团队、设计团队与远程团队用户在做日常协作（梳理想法、画流程、对齐方案）时，必须**跳出当前产品**去使用 Miro、FigJam 等独立白板工具。这带来三个明确痛点：

1. **上下文割裂**：在"产品内工作"和"白板工具"之间反复切换，思路被打断，且白板产出物（便签、图）与产品内的任务、文档脱节，无法沉淀回工作流。
2. **工具过重**：Miro 等工具功能繁杂，用户只需要"便签 + 连线"这类基础能力，却要为大量用不到的功能付出学习和导航成本。
3. **协作断点**：用户需要实时多人协作，但不需要视频/音频；现有独立工具无法和我们的对象体系打通，无法"一键链接到任务、文档"。

**代价**：不解决则用户继续留在 Miro/FigJam 生态，我们的产品只是"其中一个被切换走的地方"，留存与扩展（upsell）机会流失，且竞品（更轻量的一体化协作）在蚕食我们"集成即简单"的差异化定位。

**证据**：5 次用户访谈一致指向上述三点。代表性原声：
- "切换工具太麻烦，希望在一个地方完成所有工作。"
- "Miro 功能太多，我们只需要基础的便签和连线。"
- "实时协作很重要，但不需要视频/音频。"
- "希望能直接链接到我们产品中的其他对象（任务、文档）。"
- "移动端支持不重要，主要在桌面端用。"

---

## 2. 目标（Goals）

**用户目标**
- G1：用户能在产品内（不离开当前工作区）创建并使用白板，完成便签梳理与连线，无需打开第三方工具。
- G2：团队成员可实时共同编辑同一白板，看到彼此的光标/在线状态，协作不卡顿。
- G3：白板元素能直接链接到产品内的任务、文档等对象，形成可追溯的工作关联。

**业务目标**
- G4：提升一体化协作体验，把"白板场景"从竞品拉回我们的产品，强化留存与扩展收入。
- G5：以可控的 2 人团队 / 3 个月成本，交付"比 Miro 简单、比 Jamboard 丰富"的差异化能力。

> 量化目标见第 6 节（成功指标）。

---

## 3. 非目标（Non-Goals）

- **NG1 不做视频 / 音频会议**：用户明确表示不需要，且与主产品的会议能力重叠，纳入会显著膨胀范围。
- **NG2 不做移动端适配**：用户明确"移动端不重要，主要桌面端"，MVP 与完整版均只保证桌面端（≥1280px 宽）体验，移动端仅要求"可打开不可崩"，不做交互优化。
- **NG3 不做高级图形 / 无限画布特效**：不提供 Miro 式的自由插画、高级图表、复杂图形库、思维导图自动布局等。保持"便签 + 连线 + 基础形状"的克制。
- **NG4 不做独立白板产品 / 对外分享链接（MVP）**：白板首先服务于"已登录的团队工作区内部协作"，对外匿名分享、公开嵌入不在本期范围（Q3 再评估）。
- **NG5 不做 AI 自动生成内容（MVP）**：如 AI 自动整理便签、生成流程图不在本期，避免与核心目标（简单、实时、集成）争夺资源。

---

## 4. 用户角色与画像（Personas）

| 角色 | 典型场景 | 核心诉求 |
|---|---|---|
| 产品团队（PM / PO） | 需求梳理、用户旅程、优先级排序 | 快速贴便签、按主题分簇、连线表达依赖，并链到任务 |
| 设计团队（UX / 视觉） | 概念发散、流程草图、评审标注 | 便签 + 连线 + 基础形状，实时评审与评论 |
| 远程团队（跨地域成员） | 异步 + 实时同步脑暴 | 实时光标/在线状态、低延迟、不依赖视频 |

---

## 5. 用户故事（User Stories）

> 格式：作为 [角色]，我想要 [能力]，以便 [价值]。按优先级排序，最重要的在前。

### 5.1 产品团队

- **US-P1**：作为产品经理，我想要在工作区内一键创建白板，以便不必切换到 Miro 就能开始梳理需求。
- **US-P2**：作为产品经理，我想要在白板上添加、编辑、改色、移动和删除便签，以便组织我的想法。
- **US-P3**：作为产品经理，我想要用连线把两个便签/对象连起来并标注关系，以便表达任务依赖或流程顺序。
- **US-P4**：作为产品经理，我想要把一个便签或图形直接链接到产品内的某个任务，以便白板结论能回溯到可执行工作项。
- **US-P5**：作为产品经理，我想要看到哪些团队成员正在看这块白板，以便知道该不该现在发起讨论。
- **US-P6（P1）**：作为产品经理，我想要把白板的某块区域导出为图片贴进需求文档，以便沉淀结论。

### 5.2 设计团队

- **US-D1**：作为设计师，我想要在白板上画基础形状（矩形/圆/箭头）和自由线条，以便表达界面草图或流程。
- **US-D2**：作为设计师，我想要对某个便签或图形发起评论/批注，以便在评审中留下反馈而不破坏画面。
- **US-D3**：作为设计师，我想要实时看到协作者的光标和正在编辑的元素，以便避免和他人"撞车"。
- **US-D4**：作为设计师，我想要把白板链接到某个设计文档/规范，以便草图与正式文档关联。

### 5.3 远程团队

- **US-R1**：作为远程成员，我想要多人同时编辑同一白板且改动实时同步，以便跨时区也能一起脑暴。
- **US-R2**：作为远程成员，我想要在别人移动/新增元素时立即看到更新（无手动刷新），以便保持对齐。
- **US-R3**：作为远程成员，我想要知道谁在线、谁正在编辑哪块，以便协调分工。
- **US-R4（P1）**：作为远程成员，我想要撤销/重做我的操作，以便在误删时快速恢复。
- **US-R5（P2）**：作为远程成员，我想要查看白板的版本历史/快照，以便在争论"之前那版方案"时能找回。

### 5.4 通用 / 边界

- **US-G1**：作为用户，白板在我离开后重新打开时内容仍在（自动保存），以便不必手动导出。
- **US-G2**：作为用户，当网络中断时我的本地操作被暂存，恢复连接后自动合并，以便弱网下不丢工作。
- **US-G3**：作为用户，空白板有引导提示（如何加便签、如何连线），以便第一次用不懵。
- **US-G4**：作为用户，删除白板或清空内容前有确认，以便避免误删团队资产。

---

## 6. 需求规格（Requirements）

### 6.1 MVP（Q2，P0 / P1）

#### P0 — 必须有（砍掉任一则 MVP 不成立）

**R1 白板生命周期**
- 在工作区内创建、命名、打开、删除白板；沿用现有团队/工作区权限。
- 自动保存（本地+服务端），重开即恢复。
- 验收：Given 用户在有权限的工作区，When 点击"新建白板"，Then 生成空白板并进入编辑态；刷新/重开内容一致。

**R2 基础画布**
- 无限/大画布，支持平移（拖拽空白）、缩放（滚轮/快捷键）；坐标系统稳定。
- 验收：Given 画布上有内容，When 缩放/平移，Then 元素相对位置保持，不漂移、不模糊。

**R3 便签（Sticky Notes）**
- 创建/编辑文字、改色（预设 5–6 色）、移动、删除；支持多选与批量移动。
- 验收：Given 画布，When 双击新建便签并输入，Then 文字即时保存；改色后所有协作者同步可见。

**R4 连线（Connectors）**
- 在两个便签/形状间建立连线，可加文字标签；删除任一端自动处理连线。
- 验收：Given 两个便签，When 从 A 拉连线到 B，Then 出现可标注的连线；移动 A/B 连线跟随。

**R5 实时协作（基于现有时钟/同步架构）**
- 多人同时编辑，元素增删改实时同步；显示协作者在线状态与光标位置；无手动刷新。
- 验收：Given 用户甲、乙同开一板，When 甲新增便签，Then 乙在 <1s（目标）内看到；光标位置实时呈现。

**R6 对象链接（产品内）**
- 白板元素可链接到产品内对象（任务、文档），点击跳转；来源对象显示"被白板引用"。
- 验收：Given 一个任务，When 在白板中将该任务链接到便签，Then 便签显示链接图标，点击跳转到任务；任务侧可见反向引用。

**R7 权限与可见性**
- 白板继承工作区/团队的查看与编辑权限；无权限者不可见内容。
- 验收：Given 访客角色，When 访问受限白板，Then 被拒绝或仅只读（按现有策略）。

#### P1 — 应有（MVP 内尽量做，否则 Q3 早期补齐）

- **R8 基础形状与自由绘制**：矩形、圆、箭头、自由画笔（轻量）。
- **R9 评论/批注**：对元素发起 threaded 评论，不污染画面。
- **R10 撤销/重做**：基于操作栈，至少支持本用户最近 N 步。
- **R11 白板列表与搜索**：工作区内列出白板，支持按名称搜索、最近打开。

### 6.2 完整版（Q3，P2 / 未来）

- **R12 模板库**：空白板、用户旅程、优先级矩阵、流程图的起步模板。
- **R13 导出**：PNG / PDF 导出（用于沉淀与汇报）。
- **R14 反向嵌入**：把任务/文档的实时预览卡片嵌入白板（双向链接体验）。
- **R15 版本历史 / 快照**：时间线回放与版本对比。
- **R16 @提及与通知**：在白板内 @成员，触发现有通知渠道。
- **R17 只读分享 / 访客链接（评估）**：对外只读分享，需安全与合规评估（见第 8 节法律）。

---

## 7. 成功指标（Success Metrics）

### 7.1 滞后指标（Lagging，周/季度评估）— 来自业务目标

| 指标 | 目标 | 测量方式 | 评估时点 |
|---|---|---|---|
| 周活跃团队白板使用率 | ≥30% 的活跃团队每周至少使用一次白板 | 工作区内"过去 7 天有 ≥1 次编辑会话的团队 / 活跃团队" | 上线后每 4 周 |
| 白板会话平均时长 | >15 分钟 | 单会话（打开→关闭/无操作超时）时长均值 | 上线后每月 |
| 竞品迁移团队占比 | >20% 的白板团队来自 Miro/FigJam 等 | 入驻/首次使用问卷或 SSO/导入痕迹标记 | Q3 末 |

### 7.2 领先指标（Leading，天/周即可见，用于早期纠偏）

- **L1 创建率**：上线首周，≥40% 的工作区至少创建 1 块白板。
- **L2 激活率**：创建白板的用户中，≥70% 在首个会话内完成"加便签 + 连线"核心动作。
- **L3 协作形成率**：新建白板中，≥25% 在 7 天内出现 ≥2 名不同编辑者（说明真协作而非单人）。
- **L4 首次会话时长**：首会话中位时长 ≥5 分钟（深度使用的早期信号）。
- **L5 链接采用率**：使用白板的团队中，≥30% 至少建立 1 条到任务/文档的链接（验证"集成"价值假设）。
- **L6 错误/失败率**：白板加载失败率 <2%，实时同步冲突导致的丢失操作 <0.5%。

### 7.3 阈值建议
- **达标线（success）**：滞后指标全部达到目标。
- **拉伸线（stretch）**：周活跃率 40%、平均时长 20 分钟、迁移率 30%。

---

## 8. 技术考虑与依赖（Technical Considerations & Dependencies）

### 8.1 与现有实时同步架构兼容（硬约束）
- 白板本质是"节点（便签/形状）+ 边（连线）"的图结构，需确认现有同步层（CRDT/OT/自研）能否高效处理：
  - 并发创建/移动/删除节点；
  - 边随端点移动而更新；
  - 大画布下只同步可见/变更区域，控制带宽。
- **待确认**：现有同步协议是否支持任意结构化对象图，还是仅限文本/文档？若仅限文档型，需扩展或引入白板专用同步适配层（风险点，见第 9 节）。

### 8.2 渲染选型
- 建议基于 Canvas 的渲染库（如 Konva / Fabric.js）而非纯 DOM/SVG，便签数量多时性能更稳。
- 协作者光标层、连线层、便签层分离，降低重绘成本。

### 8.3 数据模型与持久化
- 白板作为现有"对象体系"的一等对象；节点/边以实体存储，复用现有对象 ID 与引用机制，使"链接到任务/文档"自然成立。
- 自动保存：本地快照 + 服务端持久化，断线重连后合并。

### 8.4 权限
- 复用现有团队/工作区权限中间件，白板不引入新权限模型（避免安全债）。

### 8.5 依赖团队 / 系统
- 实时同步服务团队（架构兼容评审 + 必要扩展）。
- 对象/链接系统团队（白板对象建模、反向引用）。
- 权限系统团队（复用确认）。
- 基础设施（画布渲染库选型与打包体积评估，注意桌面端为主）。

### 8.6 范围与人力现实
- 2 人 / 3 个月同时覆盖 MVP + 完整版偏紧；必须守住 P0 边界，P1 可弹性后置，P2 严格留给 Q3。
- 风险：若同步层需较大改造，P0 的 R5/R6 会吃满工期——需在第 9 节开放问题中优先澄清。

---

## 9. 开放问题（Open Questions）

| # | 问题 | 归属 | 是否阻塞 | 备注 |
|---|---|---|---|---|
| Q1 | 现有实时同步架构是否支持白板图结构（节点/边）的并发编辑？不支持则需多少改造？ | 工程 | **阻塞** | 决定 R5/R6 可行性与工期，必须在 MVP 启动前回答 |
| Q2 | 同步层在大画布、多人高频移动下的延迟与带宽上限是多少？<1s 目标能否达成？ | 工程 | 非阻塞 | 影响 L6 与体验目标 |
| Q3 | 白板对象在数据模型中如何建模才能复用现有任务/文档链接？ | 工程 | 非阻塞 | 影响 R6 实现成本 |
| Q4 | 撤销/重做（R10）在多人实时场景下如何界定"本用户操作"与"他人操作"？ | 工程/设计 | 非阻塞 | 多光标下的 UX 约定 |
| Q5 | 白板画布的交互范式（平移/缩放/多选）是否有现成设计规范可复用？ | 设计 | 非阻塞 | 保证桌面端一致性 |
| Q6 | 协作者光标/在线状态在密集便签下如何呈现才不遮挡内容？ | 设计 | 非阻塞 | 视觉与可用性 |
| Q7 | 对外只读分享（R17）是否触碰数据合规/客户合同？是否需要审计日志？ | 法律 | 非阻塞（仅 Q3） | 决定 R17 能否进完整版 |
| Q8 | 白板内容是否算"客户数据"需纳入现有数据保留/导出合规流程？ | 法律/数据 | 非阻塞 | 与现有合规对齐 |
| Q9 | 如何可靠识别"来自 Miro/FigJam 的迁移团队"（R 迁移指标）？靠问卷还是导入功能？ | 数据 | 非阻塞 | 影响迁移率测量准确性 |
| Q10 | 会话时长/活跃团队的统一定义口径（打开即算？有编辑才算？） | 数据 | 非阻塞 | 影响所有滞后指标口径 |

---

## 10. 范围与时间线建议

- **Q2（MVP）**：R1–R7（P0）+ 尽量 R8–R11（P1）。以"能创建、能贴便签连线、能实时协作、能链任务"为不可妥协底线。
- **Q3（完整版）**：R12–R17（P2），其中 R17 需法律评估通过。
- **硬约束**：MVP 发布窗口为 Q2；完整版为 Q3。任何 scope 增加须等价移除或顺延至 Q3。
- **建议节奏**：第 1–2 周先由工程确认 Q1（同步兼容性），并行设计画布交互规范；确认后再铺开 P0 实现，避免返工。

---

### 附录：与竞品的定位
- **比 Miro 简单**：砍掉高级图形/插画/自动布局/视频，只保留"便签 + 连线 + 基础形状 + 实时 + 集成链接"。
- **比 Google Jamboard 丰富**：Jamboard 仅便签+少量笔；我们增加连线标注、对象链接、评论、列表/搜索、版本历史（Q3）。

> 评审前请优先回答第 9 节的 Q1（工程阻塞项）。
